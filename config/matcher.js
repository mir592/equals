//muestra las que no ha respondido
/*
for (recorremos el array de preguntas buscamos su id)
  var id1 => metes el id
  for(recorremos las preguntas respondidas por el suario)
    var id2 0> metes el id de la que ya has respondido

    comparacion if(id1 === id2)
  */  
